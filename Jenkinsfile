pipeline {
  agent any
  stages {
    stage ('make .zip') {
      steps {
        //script {
        //  zip archive: true, dir: '', glob: '', zipFile: 'ThalesMee-1.0.0.zip'
        //}
        sh "docker-compose up "
      }
    }
  }
}
